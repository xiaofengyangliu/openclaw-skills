# wps-downloader environment variables
# This skill does not require API keys, but you can set the following if needed

# Default download directory (optional)
# WPS_DOWNLOAD_DIR=/path/to/downloads

# Puppeteer settings (optional)
# PUPPETEER_EXECUTABLE_PATH=/path/to/chrome
